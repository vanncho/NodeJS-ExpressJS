import { EventDateFormatterPipe } from './event-date-formatter.pipe';

describe('EventDateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
